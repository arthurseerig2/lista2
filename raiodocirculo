#include <stdio.h>
 
int main(void) {
	float raio, pi=3.14, area;
	
	printf("Digite o valor do raio do circulo = ");  scanf("%f", &raio);
 
	area = pi * raio * raio;
 
	printf("Area do circulo = %.2f \n", area);
 
	return 0;
}
